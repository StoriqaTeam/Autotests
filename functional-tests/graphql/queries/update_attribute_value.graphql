mutation updateAttributeValue($input: UpdateAttributeValueInput!) {
 updateAttributeValue(input: $input) {
    rawId
    attrRawId
    code
    translations {
        lang
        text
    }
    attribute {
        rawId
    }
 }
}
