mutation createStoreSubscription($input: CreateStoreSubscriptionInput!) {
  createStoreSubscription(input: $input) {
    id
    storeId
    currency
    value
    walletAddress
    trialStartDate
    trialEndDate
    status
  }
}
