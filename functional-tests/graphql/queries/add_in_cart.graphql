mutation addInCart($input: AddInCartInput!) {
  AddInCart(input: $input) {
    id
    productsCost
    productsCostWithoutDiscounts
    couponsDiscounts
    deliveryCost
    totalCost
    totalCostWithoutDiscounts
    totalCount
 }
}
